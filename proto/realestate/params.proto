syntax = "proto3";
package pratikasr.realestate.realestate;

import "gogoproto/gogo.proto";

option go_package = "github.com/pratikasr/real-estate/x/realestate/types";

// Params defines the parameters for the module.
message Params {
  option (gogoproto.goproto_stringer) = false;
  
}